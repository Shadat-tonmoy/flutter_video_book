import 'package:flutter/material.dart';

class AppColors {
  static const linkedInColor = Color(0xff0072B1);
  static const facebookColor = Color(0xff3B5998);
  static const googleColor = Colors.red;
  static Color primaryColor = ThemeData.light(useMaterial3: false).primaryColor;
  static Color primaryColor100 = ThemeData.light(useMaterial3: false).primaryColor.withOpacity(0.25);
}
